$(document).ready(function() {
	//TODO tune grunt to uglify this script
	//and appent it to concrete page, because it is
	//not very good idea to insert this code
	//to all pages
	if($(".book-index").length > 0) {}
});